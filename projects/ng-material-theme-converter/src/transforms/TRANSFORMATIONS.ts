import { TRANSFORMS } from "./impl";

export const TRANSFORMATIONS = [
  // new ComponentBackgroundColorTransform(),
  // new ComponentForegroundColorTransform(),
  // new ComponentRippleTransform(),
  // new ComponentOpacityTransform(),
  //new ComponentDensityTransform(),

  TRANSFORMS,
].flat();
